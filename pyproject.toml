[project]
name = "flight-deal-system"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.9"
dependencies = [
    "anthropic>=0.109.1",
    "fast-flights>=2.2",
    "python-dotenv>=1.2.1",
    "pyyaml>=6.0.3",
]
