---
phase: 03-reliable-daily-updates
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - web/lib/freshness.ts
  - web/components/DestinationCard.tsx
  - web/components/Header.tsx
  - web/app/page.tsx
autonomous: true
requirements: [DAILY-02]
must_haves:
  truths:
    - "Each destination card shows a real per-route freshness label derived from scanned_at (e.g. 更新於 N 日前 / 今日更新)"
    - "The global last-updated badge in the header renders the real meta.last_updated_stream_a timestamp in a human-readable form"
    - "next build succeeds with no type errors"
  artifacts:
    - path: "web/lib/freshness.ts"
      provides: "pure formatter: ISO timestamp → 廣東話 relative-age label"
      contains: "更新於"
    - path: "web/components/DestinationCard.tsx"
      provides: "per-route freshness label using newest scanned_at across the dest's months"
      contains: "freshness"
  key_links:
    - from: "web/components/DestinationCard.tsx"
      to: "CheapFlight.scanned_at"
      via: "max(scanned_at) across flights → freshness formatter"
      pattern: "scanned_at"
    - from: "web/components/Header.tsx"
      to: "meta.last_updated_stream_a"
      via: "freshness formatter on the global timestamp"
      pattern: "last_updated_stream_a"
---

<objective>
Make the website tell the truth about freshness (DAILY-02 UI half). The data plumbing already exists — `CheapFlight.scanned_at` is in web/lib/types.ts (L29), `getCheapFlights` already `select("*")` so scanned_at is fetched, and `getMeta()` already reads meta. What's missing is the DISPLAY: cards show no per-route age, and the header prints the raw timestamp string verbatim.

After 03-01 the scanned_at column actually advances on every refresh, so a per-route "更新於 N 日前" becomes meaningful — even when daily only refreshes the stalest slice, each card honestly shows its own age (the core DAILY-02 promise: honest freshness when "daily" only touches part of the route set).

Purpose: per-route + global honest freshness on the live site.
Output: a pure freshness formatter, per-card age label, a readable global badge, next build green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-reliable-daily-updates/03-CONTEXT.md
@web/AGENTS.md
@web/lib/types.ts
@web/lib/data.ts
@web/components/DestinationCard.tsx
@web/components/Header.tsx
@web/app/page.tsx
</context>

<artifacts_this_phase_produces>
This plan adds:
- `web/lib/freshness.ts` — NEW. `freshnessLabel(iso: string | null): string` returning 廣東話 relative age: null/empty → "更新時間未知"; <24h → "今日更新"; 1 day → "更新於 1 日前"; N days → "更新於 N 日前". Pure, no React, unit-friendly.
- `web/components/DestinationCard.tsx` — computes newest scanned_at across the dest's flights and renders the label in the summary (near the "N 個最平日" line).
- `web/components/Header.tsx` — renders freshnessLabel(meta.last_updated_stream_a) instead of the raw string.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Create pure freshness formatter (web/lib/freshness.ts)</name>
  <files>web/lib/freshness.ts</files>
  <read_first>
    - web/AGENTS.md (MANDATORY: this Next.js has breaking changes — read web/node_modules/next/dist/docs/ before writing web code)
    - web/lib/data.ts L36-42 (getMeta shape: Record<string,string>)
    - web/lib/types.ts L14-30 (CheapFlight.scanned_at: string | null)
  </read_first>
  <action>
    BEFORE writing: read the relevant guide(s) under web/node_modules/next/dist/docs/ per web/AGENTS.md (this is a non-standard Next.js build; confirm there are no constraints on plain lib modules / "use client" needs — a pure formatter needs neither but confirm).
    Create web/lib/freshness.ts exporting `freshnessLabel(iso: string | null | undefined): string`:
      - falsy / unparseable → "更新時間未知"
      - compute whole-day diff between now and the parsed Date (Math.floor of ms/86400000)
      - diff <= 0 → "今日更新"
      - diff === 1 → "更新於 1 日前"
      - else → `更新於 ${diff} 日前`
    Keep it dependency-free (no date libs). This is server-rendered (page is force-dynamic) so `new Date()` at render time is fine; do NOT mark "use client".
  </action>
  <verify>
    <automated>cd /Users/alvinlai/Downloads/flight-deal-system/web && PATH="/usr/local/bin:$PATH" npx tsc --noEmit -p tsconfig.json 2>&1 | grep -v 'node_modules' | grep -c 'freshness.ts' | grep -qx 0 && echo "freshness.ts typechecks clean"</automated>
  </verify>
  <done>web/lib/freshness.ts exists, exports freshnessLabel, returns the 廣東話 labels above, and contributes zero type errors.</done>
</task>

<task type="auto">
  <name>Task 2: Wire per-route + global freshness into UI; next build green</name>
  <files>web/components/DestinationCard.tsx, web/components/Header.tsx, web/app/page.tsx</files>
  <read_first>
    - web/AGENTS.md (read docs before editing)
    - web/components/DestinationCard.tsx L30-104 (props: flights: CheapFlight[]; summary block at L94-102 where the "N 個最平日 · stayRange" line lives)
    - web/components/Header.tsx L1-16 (currently prints raw `updated` string at L11-13)
    - web/app/page.tsx L31-34 (Header already receives meta; DestinationCard receives flights=g.fs)
  </read_first>
  <action>
    BEFORE editing web files: read the relevant web/node_modules/next/dist/docs/ guide per web/AGENTS.md.
    DestinationCard.tsx: import freshnessLabel from "@/lib/freshness". Compute the freshest timestamp across the dest's flights: `const lastScan = flights.map(f => f.scanned_at).filter(Boolean).sort().at(-1) ?? null;` (lexical sort works for ISO). Render `freshnessLabel(lastScan)` as a small muted line inside the summary's right-hand block (under the existing "{cheapCount} 個最平日…" div at L99-101), e.g. an extra `<div className="text-[10px] opacity-50">{freshnessLabel(lastScan)}</div>`. Do NOT change the price/tier logic.
    Header.tsx: import freshnessLabel; replace the raw `{updated}` render (L11-13) with `最後更新:{freshnessLabel(meta.last_updated_stream_a || meta.last_updated_stream_b || null)}`. Keep the conditional-render guard so nothing shows when both are empty.
    page.tsx: no logic change needed (meta + flights already flow through); only touch it if an import/prop adjustment is required.
    Respect CLAUDE.md 鐵律: only NEXT_PUBLIC_* values reach the client; this task adds no new env usage.
  </action>
  <verify>
    <automated>cd /Users/alvinlai/Downloads/flight-deal-system/web && PATH="/usr/local/bin:$PATH" npx next build 2>&1 | tail -20 | grep -Eq 'Compiled successfully|✓ Compiled' && echo BUILD_OK</automated>
  </verify>
  <done>Cards show a real per-route age label from scanned_at; header shows a human-readable global last-updated; `next build` compiles successfully with no type errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Supabase (public-read) → Next.js server render | scanned_at / meta values read with publishable key, rendered to HTML |
| Server-rendered HTML → browser | freshness labels are derived, non-secret text |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Information Disclosure | web client env | mitigate | Only NEXT_PUBLIC_* (URL + publishable key) reach the client; this plan adds no env access and no secret-key usage (CLAUDE.md 鐵律 3) |
| T-03-05 | Tampering | malformed/absent scanned_at | mitigate | freshnessLabel returns "更新時間未知" for null/unparseable inputs; no throw on bad data |
| T-03-SC | Tampering | npm installs | accept | No new npm dependencies (uses built-in Date); next/react already vendored in web/node_modules |
</threat_model>

<verification>
- `npx next build` (cwd web/) compiles successfully — proves types + server-render of the new labels.
- Grep proof of wiring: `grep -c freshnessLabel web/components/DestinationCard.tsx web/components/Header.tsx` ≥ 2; `grep -c last_updated_stream_a web/components/Header.tsx` ≥ 1.
- freshness.ts contains the 廣東話 label string 更新於.
</verification>

<success_criteria>
- Per-destination card shows honest "更新於 N 日前 / 今日更新" from scanned_at (DAILY-02 per-route).
- Header shows readable global last-updated from meta.last_updated_stream_a (DAILY-02 global).
- next build green.
</success_criteria>

<output>
Create `.planning/phases/03-reliable-daily-updates/03-02-SUMMARY.md` when done.
</output>
